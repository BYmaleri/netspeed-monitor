name: Build & Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install psutil pyinstaller pillow

<<<<<<< HEAD
      # ── 5. EXE derle ──────────────────────────────────────────────────
      - name: Build EXE with PyInstaller
=======
      - name: Build EXE
>>>>>>> f418fae (fix: workflow guncellendi)
        run: pyinstaller netspeed.spec

      - name: Install Inno Setup
        run: choco install innosetup --yes --no-progress

      - name: Build installer
        run: '& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\setup.iss'
        shell: pwsh

      - name: Get version
        id: ver
        run: echo "tag=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          name: "NetSpeed Monitor v${{ steps.ver.outputs.tag }}"
          files: installer/output/NetSpeedMonitor_Setup_v*.exe